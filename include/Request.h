#pragma once
#include <string>
#include <iostream>
#include <list>

using namespace std;

class Request{
    private:
        const string request_id;
        const string client_id; //optional
        const int requested_floor;
        //optional - direction
    public:

};